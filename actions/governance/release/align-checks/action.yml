name: Align governed release checks
description: Resolve the successful candidate aggregate and reconcile only its governed check bindings.
inputs:
  request-json:
    description: Typed release governance request.
    required: true
  token:
    description: Scoped branch governance credential.
    required: true
runs:
  using: node24
  main: dist/index.js
