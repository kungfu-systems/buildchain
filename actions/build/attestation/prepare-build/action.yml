name: Admit exact signed build bytes for hosted attestation
description: Admit exact signed build bytes for hosted attestation.
inputs:
  plan:
    description: Rooted build plan bound to this run and runtime
    required: true
  token:
    description: Scoped artifact read token
    required: true
outputs:
  subject:
    description: Verified subject path
  predicate:
    description: Build predicate path
runs:
  using: node24
  main: dist/index.js
