name: Pipeline product apply
description: Execute the internal apply product node.
inputs:
  context:
    description: Internal source-bound publication context.
    required: true
  token:
    description: Scoped provider credential.
    required: true
outputs:
  stable-wait:
    description: Retained time-only stable qualification recheck before provider effects.
runs:
  using: node24
  main: dist/index.js
