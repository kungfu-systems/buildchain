name: Pipeline version build
description: Execute the internal build version material boundary.
inputs:
  context:
    description: Retained exact version or publication context.
    required: true
  platform:
    description: Declared product platform.
    required: true
runs:
  using: node24
  main: dist/index.js
