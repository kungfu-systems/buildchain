name: locate-controller
description: Admit exact publication authority and bind independently verified evidence.
runs:
  using: node24
  main: dist/index.js
outputs:
  controller-artifact:
    description: Verified controller-artifact.
