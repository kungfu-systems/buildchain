name: web-preview-apply
description: Execute the web preview-apply capability within its caller job boundary.
inputs:
  request-json:
    description: Typed public workflow request.
    required: true
  needs-runtime-outputs-runtime-sha:
    description: Caller needs.runtime.outputs.runtime-sha value.
    required: true
  source-checkout-outcome:
    description: Actual preceding source checkout outcome.
    required: true
outputs:
  preview-apply-result-json:
    description: preview-apply-result-json node result.
    value: ${{ steps.apply.outputs.web-surface-apply-result-json }}
runs:
  using: composite
  steps:
    - name: Admit source checkout
      id: source-boundary
      if: ${{ always() }}
      uses: ./.buildchain/workflow-shell/actions/build/source/admit
      with:
        source-checkout-outcome: ${{ inputs.source-checkout-outcome }}
    - name: Checkout Buildchain runtime
      uses: actions/checkout@v7.0.0
      with:
        repository: ${{ fromJSON(inputs.request-json).buildchain-repository }}
        ref: ${{ fromJSON(inputs.needs-runtime-outputs-runtime-sha) }}
        path: .buildchain/runtime
        persist-credentials: false
    - name: Prepare exact Buildchain Node runtime
      uses: ./.buildchain/workflow-shell/actions/runtime/environment/prepare
      with:
        directory: .buildchain/runtime
    - name: Download web-surface plan
      uses: actions/download-artifact@v7.0.0
      with:
        name: buildchain-web-surface-plans
        path: .buildchain/downloaded-plans
    - name: Download web-surface artifact
      uses: actions/download-artifact@v7.0.0
      with:
        name: buildchain-web-surface-artifact
        path: ${{ fromJSON(inputs.request-json).working-directory }}/${{
          fromJSON(inputs.request-json).artifact-path }}
    - name: Configure preview AWS credentials
      uses: ./.buildchain/workflow-shell/actions/providers/aws/configure
      with:
        role: ${{ fromJSON(inputs.request-json).preview-aws-role-arn }}
        region: ${{ fromJSON(inputs.request-json).aws-region }}
        session-name: buildchain-preview-${{ github.run_id }}
    - name: Apply and verify channel transaction
      id: apply
      uses: ./.buildchain/runtime/actions/web/deployment/apply
      with:
        request-json: ${{ inputs.request-json }}
        channel: preview
    - name: Comment preview URL
      if: ${{ fromJSON(inputs.request-json).preview-comment }}
      uses: ./.buildchain/workflow-shell/actions/web/feedback/preview
      with:
        cleanup: "false"
        token: ${{ github.token }}
    - name: Upload preview apply diagnostics
      if: ${{ always() }}
      uses: actions/upload-artifact@v7.0.1
      with:
        name: buildchain-web-surface-preview-diagnostics
        path: |
          .buildchain/web-surface-preview-apply.json
          .buildchain/web-surface-preview-health.json
        include-hidden-files: true
        if-no-files-found: warn
    - name: Report observed deployment
      if: ${{ always() }}
      uses: ./.buildchain/runtime/actions/web/deployment/report
      with:
        request-json: ${{ inputs.request-json }}
        channel: preview
        job-status: ${{ job.status == 'cancelled' && 'cancelled' || github.action_status == 'failure' && 'failure' || 'success' }}
