name: select adopter qualification
description: Admit exact consumer policy or qualify public delivery and platform
  evidence through named domain APIs.
inputs:
  request-json:
    description: request-json admitted adopter input.
    required: true
  workflow-sha:
    description: workflow-sha admitted adopter input.
    required: true
outputs:
  repository:
    description: repository adopter evidence.
  sha:
    description: sha adopter evidence.
runs:
  using: node24
  main: dist/index.js
