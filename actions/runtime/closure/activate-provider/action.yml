name: Activate admitted provider closure
description: Verify immutable runtime identities before installing and exposing
  provider dependencies.
inputs:
  sha:
    description: Admitted immutable runtime commit.
    required: true
  tree:
    description: Admitted immutable runtime tree.
    required: true
runs:
  using: node24
  main: dist/index.js
