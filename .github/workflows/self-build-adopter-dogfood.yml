name: V4 Adopter Delivery Dogfood
on:
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  dogfood:
    uses: kungfu-systems/buildchain/.github/workflows/public-build-adopter-qualification.yml@v4
    with:
      consumer: buildchain
      input-path: contracts/fixtures/v4-adopter-delivery-v1/gate-positive.json
