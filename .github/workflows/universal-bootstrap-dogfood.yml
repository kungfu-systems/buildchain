name: Universal Bootstrap Self-Dogfood

on:
  workflow_dispatch:
    inputs:
      request-json:
        description: "Versioned universal request envelope"
        required: true
        type: string

permissions:
  actions: write
  artifact-metadata: write
  attestations: write
  checks: write
  contents: write
  deployments: write
  id-token: write
  issues: write
  packages: write
  pages: write
  pull-requests: write
  security-events: write
  statuses: write

jobs:
  public-bootstrap:
    name: Exercise the consumer-owned recovery contract
    uses: ./.github/workflows/universal-bootstrap-recovery.yml
    with:
      request-json: ${{ inputs.request-json }}
    secrets: inherit
