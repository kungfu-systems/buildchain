# Generated by scripts/generate-channel-promotion-workflow.mjs. Do not edit directly.
name: Release Candidate Promote

on:
  workflow_call:
    inputs:
      buildchain-channel:
        description: "Promotion workflow-shell/runtime channel: auto, alpha, or stable"
        default: "auto"
        type: string
        required: false
      buildchain-alpha-contract-lock-path:
        description: "Consumer-owned contract lock selected for alpha promotion"
        default: ".buildchain/alpha-contract-lock.json"
        type: string
        required: false
      buildchain-stable-contract-lock-path:
        description: "Consumer-owned contract lock selected for stable promotion"
        default: ".buildchain/contract-lock.json"
        type: string
        required: false
      buildchain-repository:
        default: "kungfu-systems/buildchain"
        type: string
        required: false
      buildchain-ref:
        description: "Optional Buildchain runtime ref. Empty uses the workflow shell ref."
        default: ""
        type: string
        required: false
      buildchain-contract-lock-path:
        description: "Consumer-owned Buildchain contract lock path"
        default: ""
        type: string
        required: false
      buildchain-contract-compatibility-policy:
        description: "Contract compatibility policy: major-compatible, allow-additive, or exact"
        default: "major-compatible"
        type: string
        required: false
      buildchain-contract-drift-issue-mode:
        description: "Consumer issue behavior for Buildchain floating-ref contract drift: off, breaking-only, or compatible-and-breaking"
        default: "compatible-and-breaking"
        type: string
        required: false
      allow-repository:
        description: "Repository allowed to promote. Empty defaults to the caller repository."
        default: ""
        type: string
        required: false
      channel:
        description: "Promotion channel: alpha, release, or major. Empty derives from target-ref."
        default: ""
        type: string
        required: false
      target-ref:
        description: "Channel ref to promote. Defaults to the current ref name."
        default: ""
        type: string
        required: false
      target-sha:
        description: "Channel commit SHA to promote. Defaults to the current workflow SHA."
        default: ""
        type: string
        required: false
      package-manager:
        description: "Package manager recorded for audit: npm, pnpm, yarn, or custom"
        default: "npm"
        type: string
        required: false
      artifact-name:
        description: "Base artifact name used by the PR-stage reusable build"
        default: "buildchain-artifact"
        type: string
        required: false
      release-candidate-workflow-file:
        description: "PR-stage release-candidate workflow file name"
        default: "build.yml"
        type: string
        required: false
      release-candidate-workflow-name:
        description: "PR-stage release-candidate workflow display name"
        default: "Build"
        type: string
        required: false
      release-candidate-wait-seconds:
        description: "Bounded wait for the complete PR-stage release-candidate workflow, including post-required-check evidence tails"
        default: 10800
        type: number
        required: false
      resume-candidate-repository:
        description: "Repository that owns the sealed candidate run; empty selects normal candidate discovery"
        default: ""
        type: string
        required: false
      resume-candidate-run-id:
        description: "Exact successful candidate workflow run ID to recover through this fresh workflow event"
        default: ""
        type: string
        required: false
      resume-expected-workflow-file:
        description: "Exact trusted Build workflow file that produced the candidate"
        default: ""
        type: string
        required: false
      resume-expected-workflow-name:
        description: "Exact trusted Build workflow display name that produced the candidate"
        default: ""
        type: string
        required: false
      resume-expected-source-tree:
        description: "Optional exact candidate Git tree; required when expected candidate root is empty"
        default: ""
        type: string
        required: false
      resume-expected-candidate-root:
        description: "Optional exact sha256 Release Candidate root; required when expected source tree is empty"
        default: ""
        type: string
        required: false
      resume-expected-candidate-runtime-sha:
        description: "Exact Buildchain runtime SHA recorded by the original candidate"
        default: ""
        type: string
        required: false
      resume-buildchain-runtime-sha:
        description: "Exact current Buildchain tooling SHA authorized to perform recovery"
        default: ""
        type: string
        required: false
      resume-transaction-id:
        description: "Optional exact durable publication transaction identity to resume"
        default: ""
        type: string
        required: false
      release-candidate-family-evidence-required:
        description: "Require exact Initiative-family evidence before protected promotion"
        default: false
        type: boolean
        required: false
      release-candidate-family-evidence-root:
        description: "Optional expected Initiative-family evidence content root"
        default: ""
        type: string
        required: false
      release-candidate-family-initiative-id:
        description: "Optional expected Initiative identity"
        default: ""
        type: string
        required: false
      release-candidate-family-assignment-id:
        description: "Optional expected child Assignment identity"
        default: ""
        type: string
        required: false
      artifact-patterns:
        description: "Newline-separated payload artifact patterns expected from the RC bundle"
        default: ""
        type: string
        required: false
      required-status-check:
        description: "Exact required protected-branch status check context"
        default: "check / check"
        type: string
        required: false
      branch-protection-bypass-apps:
        description: "GitHub App slugs allowed to bypass pull-request review for Buildchain-managed generated channel ref updates"
        default: "github-actions"
        type: string
        required: false
      branch-protection-bypass-users:
        description: "Reserved compatibility input; managed channel protection rejects user bypass actors"
        default: ""
        type: string
        required: false
      branch-protection-bypass-teams:
        description: "Reserved compatibility input; managed channel protection rejects team bypass actors"
        default: ""
        type: string
        required: false
      publish-target:
        description: "Publish target recorded for audit: npm, github-release, custom"
        default: "npm"
        type: string
        required: false
      github-release:
        description: "Create/update the public GitHub Release and upload Buildchain release passport/evidence assets after a complete release transaction"
        default: true
        type: boolean
        required: false
      declarative-release-tail:
        description: "Publish and read back GitHub Release assets through the Buildchain declarative release-tail provider plane"
        default: false
        type: boolean
        required: false
      github-release-title:
        description: "Optional GitHub Release title. Empty defaults to the public release tag."
        default: ""
        type: string
        required: false
      github-release-notes:
        description: "Optional GitHub Release notes. Empty uses a Buildchain release-passport asset note."
        default: ""
        type: string
        required: false
      github-release-payload-patterns:
        description: "Newline-separated basename patterns selecting PR-stage payload files for the immutable public GitHub Release"
        default: ""
        type: string
        required: false
      publication-commit-command:
        description: "Optional consumer command that commits the well-known publication authority after every artifact, passport, and public read-back prerequisite succeeds"
        default: ""
        type: string
        required: false
      publication-commit-evidence-path:
        description: "Consumer evidence JSON written by publication-commit-command under .buildchain/"
        default: ".buildchain/publication-commit/evidence.json"
        type: string
        required: false
      release-activation-command:
        description: "Optional consumer command that completes exact site publication/readback and writes the authoritative activation receipt set"
        default: ""
        type: string
        required: false
      release-activation-receipt-set-path:
        description: "Consumer activation receipt set written under .buildchain/"
        default: ".buildchain/release-activation/receipt-set.json"
        type: string
        required: false
      release-passport-evidence-command:
        description: "Optional consumer command that synthesizes released evidence only from the validated activation receipt set"
        default: ""
        type: string
        required: false
      release-passport-evidence-path:
        description: "Released evidence JSON written by release-passport-evidence-command under .buildchain/"
        default: ".buildchain/release-evidence/product-released-evidence.json"
        type: string
        required: false
      standalone-binary-distribution:
        description: "Dispatch binary-distribution.yml after promotion; enable only when the caller repository provides that workflow"
        default: false
        type: boolean
        required: false
      runner-preset:
        description: "Runner preset recorded for audit"
        default: "github-hosted"
        type: string
        required: false
      trusted-publishing:
        description: "Whether the publish target uses GitHub OIDC trusted publishing"
        default: true
        type: boolean
        required: false
      publish-command:
        description: "Optional publish command. Defaults to lifecycle.publish from buildchain.toml."
        default: ""
        type: string
        required: false
      publish-rematerialize-on-resume:
        description: "Replay the consumer publish materialization step after valid durable evidence is restored; use only when the command is idempotent and recreates ephemeral Passport inputs or release assets"
        default: false
        type: boolean
        required: false
      publish-transaction-override:
        description: "Explicitly allow ancestry-bound finalization recovery for an already published durable transaction"
        default: false
        type: boolean
        required: false
      publish-required-artifacts-json:
        description: "Required publish artifacts JSON passed to the promotion action; ref_template may contain one {version} placeholder"
        default: ""
        type: string
        required: false
      publish-artifact-kind:
        description: "Artifact kind used when generating publish-required-artifacts-json from downloaded payloads"
        default: "npm"
        type: string
        required: false
      publish-mode:
        description: "Publish mode passed to promote-buildchain-ref"
        default: ""
        type: string
        required: false
      publish-dist-tag:
        description: "Expected dist-tag passed to promote-buildchain-ref"
        default: ""
        type: string
        required: false
      publish-package-set-order:
        description: "Package-set ordering passed to promote-buildchain-ref"
        default: "as-provided"
        type: string
        required: false
      publish-package-main:
        description: "Main package name for package-set ordering"
        default: ""
        type: string
        required: false
      release-passport:
        description: "Generate and persist the release passport after publish finalization"
        default: true
        type: boolean
        required: false
      release-propagation-config-path:
        description: "Optional source-controlled package propagation capture config read from the exact finalized release SHA"
        default: ""
        type: string
        required: false
      release-passport-output-dir:
        description: "Release passport output directory"
        default: ".buildchain/release-passport"
        type: string
        required: false
      release-passport-product-name:
        description: "Product name recorded in buildchain.release.json"
        default: ""
        type: string
        required: false
      release-passport-impact-json:
        description: "Surface-aware release impact JSON or path passed to the release passport collector"
        default: ""
        type: string
        required: false
      release-passport-kfd-1-witness-jsons:
        description: "Newline- or comma-separated KFD-1 contract-world witness JSON paths passed to the release passport collector"
        default: ""
        type: string
        required: false
      release-passport-kfd-2-claim-jsons:
        description: "Newline- or comma-separated KFD-2 public release trust claim JSON paths passed to the release passport collector"
        default: ""
        type: string
        required: false
      release-passport-kfd-3-prebuild-witness-jsons:
        description: "Newline- or comma-separated KFD-3 collaboration-interface pre-build witness JSON paths passed to the release passport collector"
        default: ""
        type: string
        required: false
      release-passport-kfd-3-artifact-witness-jsons:
        description: "Newline- or comma-separated KFD-3 artifact-side collaboration-interface witness JSON paths passed to the release passport collector"
        default: ""
        type: string
        required: false
      release-passport-kfd-3-artifact-verify-command:
        description: "Product-owned command that emits KFD-3 artifact-side collaboration-interface witness JSON"
        default: ""
        type: string
      release-passport-kfd-adopter-manifest-json:
        description: "Standard full-cut adopter conformance manifest JSON path"
        default: ""
        type: string
        required: false
      release-passport-kfd-support-matrix-json:
        description: "Legacy KFD support projection JSON path accepted only for exact manifest comparison"
        default: ""
        type: string
        required: false
      release-passport-kfd-product-gate-jsons:
        description: "Newline- or comma-separated KFD-4/5/7 product gate result JSON paths"
        default: ""
        type: string
        required: false
      release-passport-invariant-passport-jsons:
        description: "Newline- or comma-separated invariant Passport JSON paths passed to the release gate"
        default: ""
        type: string
        required: false
      release-passport-invariant-passport-command:
        description: "Product-owned command that emits a canonical invariant Passport JSON document"
        default: ""
        type: string
        required: false
      release-passport-evidence-jsons:
        description: "Newline- or comma-separated product-owned release evidence attachment paths bound to the exact release coordinates"
        default: ""
        type: string
        required: false
      release-passport-attachment-command:
        description: "Product-owned command that emits generated release evidence attachment paths as JSON"
        default: ""
        type: string
        required: false
      release-passport-buildchain-self-kfd:
        description: "Generate Buildchain-owned KFD-1/2/3 self witnesses and claims from the checked-out package source before release passport finalization"
        default: false
        type: boolean
        required: false
      github-artifact-attestation-policy-json:
        description: "One GitHub artifact attestation policy JSON path from the sealed release candidate; empty disables attestation"
        default: ""
        type: string
        required: false
      github-artifact-attestation-environment:
        description: "Caller-repository protected environment governing the keyless attestation"
        default: "buildchain-artifact-attestation"
        type: string
        required: false
      github-artifact-attestation-retention-days:
        description: "Retention for offline GitHub/Sigstore evidence artifacts"
        default: 90
        type: number
        required: false
      release-passport-platform-manifest-paths:
        description: "Optional platform manifest paths for release passport; defaults to downloaded PR-stage payload manifests"
        default: ""
        type: string
        required: false
      required-artifact-count:
        description: "Minimum downloaded payload count required before publish; npm counts package tarballs"
        default: 3
        type: number
        required: false
      publication-admission-json:
        description: "Fresh sealed publication admission JSON; required for non-dry-run publication"
        default: ""
        type: string
        required: false
      publication-runner-provenance-json:
        description: "Runner provenance receipt bound by the admission"
        default: ""
        type: string
        required: false
      publication-control-plane-audit-json:
        description: "Fresh external control-plane audit receipt bound by the admission"
        default: ""
        type: string
        required: false
      github-governance-receipt-json:
        description: "Fresh qualifying governance receipt for the exact publication repository and target ref"
        default: ""
        type: string
        required: false
      publication-gate-aggregate-json:
        description: "Qualifying Shifu Gate aggregate or explicit consumer-owned no-Gate decision"
        default: ""
        type: string
        required: false
      publication-gate-command:
        description: "Consumer-owned read-only command that assembles a qualifying Gate aggregate from the exact downloaded release-candidate evidence"
        default: ""
        type: string
        required: false
      publication-gate-controller-sha:
        description: "Exact caller-repository commit that owns the consumer Gate command; empty uses the publication source SHA"
        default: ""
        type: string
        required: false
      publication-expected-json:
        description: "Independent exact source/runtime/product/channel/artifact bindings"
        default: ""
        type: string
        required: false
      publication-used-nonces-json:
        description: "JSON array of already consumed admission nonces"
        default: "[]"
        type: string
        required: false
      publication-consumer-qualification-command:
        description: "Opt-in consumer-owned predicate command; writes its decision to BUILDCHAIN_PUBLICATION_QUALIFICATION_RESULT_PATH"
        default: ""
        type: string
        required: false
      publication-consumer-qualification-controller-sha:
        description: "Exact caller-repository commit that owns the consumer qualification command; empty uses the Gate controller SHA, then the publication source SHA"
        default: ""
        type: string
        required: false
      publication-consumer-predicate-id:
        description: "Stable identifier for the opt-in consumer-owned publication predicate"
        default: ""
        type: string
        required: false
      publication-used-qualification-nonces-json:
        description: "JSON array of already consumed qualification nonces"
        default: "[]"
        type: string
        required: false
      publication-auto-admission:
        description: "Assemble sealed evidence from the caller repository's exact managed release candidate"
        default: false
        type: boolean
        required: false
      publication-auto-no-gate:
        description: "Explicitly declare that this managed publication has no consumer-owned Shifu Gate registry"
        default: false
        type: boolean
        required: false
      publication-publisher-workflow-path:
        description: "Caller workflow path bound by the provider trusted-publisher policy"
        default: ""
        type: string
        required: false
      publication-product:
        default: "Buildchain"
        type: string
        required: false
      publication-target:
        default: "npm:@kungfu-tech/buildchain"
        type: string
        required: false
      publication-package-name:
        default: "@kungfu-tech/buildchain"
        type: string
        required: false
      dry-run:
        description: "Plan only; do not publish or move refs"
        default: false
        type: boolean
        required: false
    secrets:
      BUILDCHAIN_PROMOTION_TOKEN:
        description: "Optional bypass-capable token used only for generated protected ref updates"
        required: false
      BUILDCHAIN_PUBLICATION_COMMIT_TOKEN:
        description: "Optional consumer-owned credential exposed only to the final publication commit command"
        required: false
      BUILDCHAIN_PUBLICATION_COMMIT_SIGNING_KEY:
        description: "Optional consumer-owned private signing material exposed only to the final publication commit command"
        required: false
      KUNGFU_GOVERNANCE_AUDITOR_APP_PRIVATE_KEY:
        description: "Private key for the dedicated read-only GitHub governance auditor App"
        required: false
      buildchain-issue-token:
        description: "Optional token with issues:write on the Buildchain repository for workflow-friction reports"
        required: false
      BUILDCHAIN_ISSUE_TOKEN:
        description: "Optional token with issues:write on the Buildchain repository for workflow-friction reports"
        required: false
      buildchain-issue-app-id:
        description: "Optional GitHub App ID used to mint a Buildchain issue-report token"
        required: false
      BUILDCHAIN_ISSUE_APP_ID:
        description: "Optional GitHub App ID used to mint a Buildchain issue-report token"
        required: false
      buildchain-issue-app-private-key:
        description: "Optional GitHub App private key used to mint a Buildchain issue-report token"
        required: false
      BUILDCHAIN_ISSUE_APP_PRIVATE_KEY:
        description: "Optional GitHub App private key used to mint a Buildchain issue-report token"
        required: false
    outputs:
      buildchain-channel:
        description: "Selected promotion workflow-shell channel"
        value: ${{ jobs.resolve-promotion.outputs.channel }}
      promotion-router-ref:
        description: "Router workflow ref"
        value: ${{ jobs.resolve-promotion.outputs.router-ref }}
      promotion-router-sha:
        description: "Resolved router workflow SHA"
        value: ${{ jobs.resolve-promotion.outputs.router-sha }}
      promotion-shell-ref:
        description: "Selected advanced promotion workflow-shell ref"
        value: ${{ jobs.resolve-promotion.outputs.shell-ref }}
      promotion-shell-sha:
        description: "Resolved advanced promotion workflow-shell SHA"
        value: ${{ jobs.resolve-promotion.outputs.shell-sha }}
      promotion-runtime-ref:
        description: "Selected Buildchain runtime ref"
        value: ${{ jobs.resolve-promotion.outputs.runtime-ref }}
      promotion-runtime-sha:
        description: "Resolved Buildchain runtime SHA"
        value: ${{ jobs.resolve-promotion.outputs.runtime-sha }}
      promotion-contract-lock-path:
        description: "Selected consumer contract-lock path"
        value: ${{ jobs.resolve-promotion.outputs.contract-lock-path }}
      promotion-contract-lock-digest:
        description: "Selected consumer contract-lock sha256 digest"
        value: ${{ jobs.resolve-promotion.outputs.contract-lock-digest }}
      promotion-publication-channel:
        description: "Derived publication channel"
        value: ${{ jobs.resolve-promotion.outputs.publication-channel }}
      promotion-target-ref:
        description: "Derived promotion target ref"
        value: ${{ jobs.resolve-promotion.outputs.target-ref }}
      promotion-override-used:
        description: "Whether a trusted runtime override was used"
        value: ${{ jobs.resolve-promotion.outputs.override-used }}
      promoted-sha:
        description: "Promoted channel SHA"
        value: ${{ jobs.alpha.outputs.promoted-sha || jobs.stable.outputs.promoted-sha }}
      built-source-sha:
        description: "PR-stage built source SHA"
        value: ${{ jobs.alpha.outputs.built-source-sha || jobs.stable.outputs.built-source-sha }}
      release-candidate-artifact:
        description: "Resolved release-candidate artifact name"
        value: ${{ jobs.alpha.outputs.release-candidate-artifact || jobs.stable.outputs.release-candidate-artifact }}
      release-candidate-action:
        description: "Candidate admission action: discovered or reused"
        value: ${{ jobs.alpha.outputs.release-candidate-action || jobs.stable.outputs.release-candidate-action }}
      release-candidate-recovery-root:
        description: "Exact sha256 recovery receipt root"
        value: ${{ jobs.alpha.outputs.release-candidate-recovery-root || jobs.stable.outputs.release-candidate-recovery-root }}
      release-candidate-recovery-receipt-artifact:
        description: "Immutable recovery receipt Actions artifact name"
        value: ${{ jobs.alpha.outputs.release-candidate-recovery-receipt-artifact || jobs.stable.outputs.release-candidate-recovery-receipt-artifact }}
      release-propagation-work-artifact:
        description: "Captured package release propagation Work artifact"
        value: ${{ jobs.alpha.outputs.release-propagation-work-artifact || jobs.stable.outputs.release-propagation-work-artifact }}
      publication-commit-evidence-path:
        description: "Validated consumer publication commit evidence path"
        value: ${{ jobs.alpha.outputs.publication-commit-evidence-path || jobs.stable.outputs.publication-commit-evidence-path }}
      publication-commit-evidence-digest:
        description: "sha256 digest of validated consumer publication commit evidence"
        value: ${{ jobs.alpha.outputs.publication-commit-evidence-digest || jobs.stable.outputs.publication-commit-evidence-digest }}
      publication-commit-url:
        description: "Canonical public authority URL validated after the final commit"
        value: ${{ jobs.alpha.outputs.publication-commit-url || jobs.stable.outputs.publication-commit-url }}
      publication-commit-payload-root:
        description: "Exact public authority payload root validated after the final commit"
        value: ${{ jobs.alpha.outputs.publication-commit-payload-root || jobs.stable.outputs.publication-commit-payload-root }}
      controller-plan-artifact:
        description: "Controller plan artifact name"
        value: ${{ jobs.alpha.outputs.controller-plan-artifact || jobs.stable.outputs.controller-plan-artifact }}
      controller-plan-json:
        description: "Compact controller plan JSON"
        value: ${{ jobs.alpha.outputs.controller-plan-json || jobs.stable.outputs.controller-plan-json }}
      controller-plan-digest:
        description: "Controller plan digest"
        value: ${{ jobs.alpha.outputs.controller-plan-digest || jobs.stable.outputs.controller-plan-digest }}
      publication-capability-digest:
        description: "Sealed publication capability receipt digest"
        value: ${{ jobs.alpha.outputs.publication-capability-digest || jobs.stable.outputs.publication-capability-digest }}
      github-artifact-attestation-id:
        description: "GitHub keyless attestation id for the declared Linux release artifact"
        value: ${{ jobs.alpha.outputs.github-artifact-attestation-id || jobs.stable.outputs.github-artifact-attestation-id }}
      github-artifact-attestation-url:
        description: "GitHub keyless attestation URL"
        value: ${{ jobs.alpha.outputs.github-artifact-attestation-url || jobs.stable.outputs.github-artifact-attestation-url }}
      github-artifact-attestation-evidence-root:
        description: "Verified Buildchain attestation evidence root published with the GitHub Release"
        value: ${{ jobs.alpha.outputs.github-artifact-attestation-evidence-root || jobs.stable.outputs.github-artifact-attestation-evidence-root }}
      github-artifact-attestation-publication-receipt-digest:
        description: "Read-back verified GitHub Release evidence publication receipt digest"
        value: ${{ jobs.alpha.outputs.github-artifact-attestation-publication-receipt-digest || jobs.stable.outputs.github-artifact-attestation-publication-receipt-digest }}
      release-tail-declaration-root:
        description: "Root of the declarative release-tail contract"
        value: ${{ jobs.alpha.outputs.release-tail-declaration-root || jobs.stable.outputs.release-tail-declaration-root }}
      release-tail-transaction-state:
        description: "Final declarative release-tail transaction state"
        value: ${{ jobs.alpha.outputs.release-tail-transaction-state || jobs.stable.outputs.release-tail-transaction-state }}
      release-tail-transaction-root:
        description: "Stable declarative release-tail transaction root"
        value: ${{ jobs.alpha.outputs.release-tail-transaction-root || jobs.stable.outputs.release-tail-transaction-root }}
      release-tail-state-root:
        description: "Root of the final declarative release-tail transaction state"
        value: ${{ jobs.alpha.outputs.release-tail-state-root || jobs.stable.outputs.release-tail-state-root }}
      release-tail-receipt-roots-json:
        description: "JSON array of standardized declarative release-tail receipt roots"
        value: ${{ jobs.alpha.outputs.release-tail-receipt-roots-json || jobs.stable.outputs.release-tail-receipt-roots-json }}
      controller-receipt-artifact:
        description: "Controller receipt artifact name"
        value: ${{ jobs.alpha.outputs.controller-receipt-artifact || jobs.stable.outputs.controller-receipt-artifact }}
      controller-receipt-json:
        description: "Compact controller receipt JSON"
        value: ${{ jobs.alpha.outputs.controller-receipt-json || jobs.stable.outputs.controller-receipt-json }}
      controller-receipt-digest:
        description: "Controller receipt digest"
        value: ${{ jobs.alpha.outputs.controller-receipt-digest || jobs.stable.outputs.controller-receipt-digest }}
      controller-receipt-status:
        description: "Controller receipt status"
        value: ${{ jobs.alpha.outputs.controller-receipt-status || jobs.stable.outputs.controller-receipt-status }}

permissions:
  actions: write
  artifact-metadata: write
  attestations: write
  checks: write
  contents: write
  id-token: write
  issues: write
  pull-requests: write

jobs:
  resolve-promotion:
    name: Resolve promotion workflow shell and runtime
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      channel: ${{ steps.route.outputs.channel }}
      publication-channel: ${{ steps.route.outputs.publication-channel }}
      target-ref: ${{ steps.route.outputs.target-ref }}
      router-ref: ${{ steps.router.outputs.ref }}
      router-sha: ${{ steps.router.outputs.sha }}
      shell-ref: ${{ steps.route.outputs.shell-ref }}
      shell-call-ref: ${{ steps.identities.outputs.shell-call-ref }}
      shell-sha: ${{ steps.identities.outputs.shell-sha }}
      runtime-ref: ${{ steps.route.outputs.runtime-ref }}
      runtime-sha: ${{ steps.identities.outputs.runtime-sha }}
      contract-lock-path: ${{ steps.lock.outputs.path }}
      contract-lock-digest: ${{ steps.lock.outputs.digest }}
      override-used: ${{ steps.route.outputs.override-used }}
      selection-source: ${{ steps.route.outputs.selection-source }}
      reason: ${{ steps.route.outputs.reason }}
    steps:
      - name: Resolve promotion router source
        id: router
        shell: bash
        env:
          BUILDCHAIN_ROUTER_REPOSITORY: ${{ inputs.buildchain-repository }}
          BUILDCHAIN_ROUTER_REF: ${{ inputs.buildchain-ref }}
          BUILDCHAIN_RESUME_RUN_ID: ${{ inputs.resume-candidate-run-id }}
          BUILDCHAIN_RESUME_RUNTIME_SHA: ${{ inputs.resume-buildchain-runtime-sha }}
        run: |
          set -euo pipefail
          repository="${BUILDCHAIN_ROUTER_REPOSITORY}"
          ref="${BUILDCHAIN_ROUTER_REF}"
          ref="${ref#refs/heads/}"
          ref="${ref#refs/tags/}"
          if [[ ! "${repository}" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$ || -z "${ref}" || "${ref}" = /* || "${ref}" = *..* ]]; then
            echo "::error::Unable to resolve promotion router source from repository/ref inputs"
            exit 1
          fi

          if [[ "${ref}" =~ ^[0-9a-fA-F]{40}$ ]]; then sha="${ref,,}"; else
            remote_url="https://github.com/${repository}.git"
            refs="$(git ls-remote "${remote_url}" "refs/heads/${ref}" "refs/tags/${ref}" "refs/tags/${ref}^{}")"
            head_sha="$(printf '%s\n' "${refs}" | awk -v name="refs/heads/${ref}" '$2 == name { print tolower($1) }')"
            tag_sha="$(printf '%s\n' "${refs}" | awk -v peeled="refs/tags/${ref}^{}" -v name="refs/tags/${ref}" '$2 == peeled { print tolower($1); found=1 } $2 == name && !found { fallback=tolower($1) } END { if (!found && fallback != "") print fallback }')"
            if [[ -n "${head_sha}" && -n "${tag_sha}" && "${head_sha}" != "${tag_sha}" ]]; then echo "::error::Promotion router ref is ambiguous between branch and tag"; exit 1; fi
            sha="${head_sha:-${tag_sha}}"
          fi
          if [[ ! "${sha}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::Promotion router ref did not resolve to one exact commit SHA"
            exit 1
          fi
          if [[ -n "${BUILDCHAIN_RESUME_RUN_ID}" ]]; then
            expected_sha="${BUILDCHAIN_RESUME_RUNTIME_SHA,,}"
            if [[ ! "${expected_sha}" =~ ^[0-9a-f]{40}$ || "${sha}" != "${expected_sha}" ]]; then
              echo "::error::Recovery router ref does not match resume-buildchain-runtime-sha"
              exit 1
            fi
          fi
          {
            echo "repository=${repository}"
            echo "ref=${ref}"
            echo "sha=${sha}"
          } >> "${GITHUB_OUTPUT}"

      - name: Checkout promotion router
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ steps.router.outputs.repository }}
          ref: ${{ steps.router.outputs.sha }}
          path: .buildchain/router
          persist-credentials: false

      - name: Resolve promotion channel
        id: route
        shell: bash
        run: >-
          node .buildchain/router/scripts/promotion-channel-router.mjs
          --cwd .buildchain/router
          --channel "${{ inputs.buildchain-channel }}"
          --buildchain-ref "${{ inputs.buildchain-ref }}"
          --publication-channel "${{ inputs.channel }}"
          --target-ref "${{ inputs.target-ref || github.ref_name }}"
          --router-ref "${{ steps.router.outputs.ref }}"
          --router-sha "${{ steps.router.outputs.sha }}"

      - name: Authorize trusted runtime override
        if: ${{ steps.route.outputs.override-used == 'true' }}
        uses: actions/github-script@v8
        with:
          script: |
            const script = require(process.env.GITHUB_WORKSPACE + "/.buildchain/router/scripts/authorize-promotion-runtime-override.cjs");
            await script.authorizePromotionRuntimeOverride({ github, context });

      - name: Resolve immutable promotion identities
        id: identities
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CHANNEL: ${{ steps.route.outputs.channel }}
          SELECTED_SHELL_REF: ${{ steps.route.outputs.shell-ref }}
          ALPHA_SHELL_REF: v4-alpha
          ALPHA_SHELL_CALL_REF: 98dc8e3d628b558bc8b35a91a821ad6c97334b77
          STABLE_SHELL_REF: v4
          STABLE_SHELL_CALL_REF: 98dc8e3d628b558bc8b35a91a821ad6c97334b77
        run: |
          set -euo pipefail
          if [[ "${CHANNEL}" = "alpha" ]]; then
            expected_ref="${ALPHA_SHELL_REF}"
            call_ref="${ALPHA_SHELL_CALL_REF}"
          else
            expected_ref="${STABLE_SHELL_REF}"
            call_ref="${STABLE_SHELL_CALL_REF}"
          fi
          if [[ "${SELECTED_SHELL_REF}" != "${expected_ref}" ]]; then
            echo "::error::Selected promotion shell ref ${SELECTED_SHELL_REF} does not match configured ${expected_ref}"
            exit 1
          fi
          node .buildchain/router/scripts/promotion-identity-resolver.mjs \
            --repository "${{ steps.router.outputs.repository }}" \
            --router-ref "${{ steps.router.outputs.ref }}" \
            --router-sha "${{ steps.router.outputs.sha }}" \
            --shell-ref "${SELECTED_SHELL_REF}" \
            --shell-call-ref "${call_ref}" \
            --runtime-ref "${{ steps.route.outputs.runtime-ref }}"

      - name: Checkout selected promotion workflow shell
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ steps.router.outputs.repository }}
          ref: ${{ steps.identities.outputs.shell-sha }}
          path: .buildchain/shell
          persist-credentials: false

      - name: Checkout selected Buildchain runtime
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ steps.router.outputs.repository }}
          ref: ${{ steps.identities.outputs.runtime-sha }}
          path: .buildchain/runtime
          persist-credentials: false

      - name: Checkout promotion source
        uses: actions/checkout@v7.0.0
        with:
          ref: ${{ inputs.target-sha || github.sha }}
          path: .buildchain/source
          persist-credentials: false

      - name: Select and verify consumer contract lock
        id: lock
        shell: bash
        env:
          EXPLICIT_PATH: ${{ inputs.buildchain-contract-lock-path }}
          ALPHA_PATH: ${{ inputs.buildchain-alpha-contract-lock-path }}
          STABLE_PATH: ${{ inputs.buildchain-stable-contract-lock-path }}
          CHANNEL: ${{ steps.route.outputs.channel }}
        run: |
          set -euo pipefail
          path="${EXPLICIT_PATH}"
          if [[ -z "${path}" ]]; then
            if [[ "${CHANNEL}" = "alpha" ]]; then path="${ALPHA_PATH}"; else path="${STABLE_PATH}"; fi
          fi
          if [[ -z "${path}" || ! -f ".buildchain/source/${path}" ]]; then
            echo "::error::Selected promotion contract lock is missing: ${path:-<empty>}"
            exit 1
          fi
          digest="sha256:$(shasum -a 256 ".buildchain/source/${path}" | awk '{print $1}')"
          echo "path=${path}" >> "${GITHUB_OUTPUT}"
          echo "digest=${digest}" >> "${GITHUB_OUTPUT}"

      - name: Verify immutable promotion checkouts
        shell: bash
        env:
          CHANNEL: ${{ steps.route.outputs.channel }}
          ALPHA_SHELL_WORKFLOW_PATH: .github/workflows/.release-candidate-promote.yml
          STABLE_SHELL_WORKFLOW_PATH: .github/workflows/.release-candidate-promote.yml
          ROUTER_SHA: ${{ steps.router.outputs.sha }}
          SHELL_SHA: ${{ steps.identities.outputs.shell-sha }}
          RUNTIME_SHA: ${{ steps.identities.outputs.runtime-sha }}
        run: |
          set -euo pipefail
          if [[ "${CHANNEL}" = "alpha" ]]; then
            workflow_path="${ALPHA_SHELL_WORKFLOW_PATH}"
          else
            workflow_path="${STABLE_SHELL_WORKFLOW_PATH}"
          fi
          test -f ".buildchain/shell/${workflow_path}"
          [[ "$(git -C .buildchain/router rev-parse HEAD)" = "${ROUTER_SHA}" ]] || { echo "::error::Promotion router checkout moved"; exit 1; }
          [[ "$(git -C .buildchain/shell rev-parse HEAD)" = "${SHELL_SHA}" ]] || { echo "::error::Promotion shell checkout moved"; exit 1; }
          [[ "$(git -C .buildchain/runtime rev-parse HEAD)" = "${RUNTIME_SHA}" ]] || { echo "::error::Promotion runtime checkout moved"; exit 1; }

  alpha:
    name: Promote with alpha workflow shell
    needs: resolve-promotion
    if: ${{ needs.resolve-promotion.outputs.channel == 'alpha' }}
    uses: kungfu-systems/buildchain/.github/workflows/.release-candidate-promote.yml@98dc8e3d628b558bc8b35a91a821ad6c97334b77
    permissions:
      actions: write
      artifact-metadata: write
      attestations: write
      checks: write
      contents: write
      id-token: write
      issues: write
      pull-requests: write
    with:
      buildchain-repository: ${{ inputs.buildchain-repository }}
      buildchain-ref: ${{ needs.resolve-promotion.outputs.runtime-sha }}
      buildchain-expected-channel: ${{ needs.resolve-promotion.outputs.channel }}
      buildchain-expected-major: "4"
      promotion-router-ref: ${{ needs.resolve-promotion.outputs.router-ref }}
      promotion-router-sha: ${{ needs.resolve-promotion.outputs.router-sha }}
      promotion-shell-ref: ${{ needs.resolve-promotion.outputs.shell-call-ref }}
      promotion-shell-sha: ${{ needs.resolve-promotion.outputs.shell-sha }}
      promotion-runtime-ref: ${{ needs.resolve-promotion.outputs.runtime-ref }}
      promotion-runtime-sha: ${{ needs.resolve-promotion.outputs.runtime-sha }}
      promotion-contract-lock-path: ${{ needs.resolve-promotion.outputs.contract-lock-path }}
      promotion-contract-lock-digest: ${{ needs.resolve-promotion.outputs.contract-lock-digest }}
      promotion-publication-channel: ${{ needs.resolve-promotion.outputs.publication-channel }}
      promotion-target-ref: ${{ needs.resolve-promotion.outputs.target-ref }}
      promotion-override-used: ${{ needs.resolve-promotion.outputs.override-used == 'true' }}
      buildchain-contract-lock-path: ${{ needs.resolve-promotion.outputs.contract-lock-path }}
      buildchain-contract-compatibility-policy: ${{ inputs.buildchain-contract-compatibility-policy }}
      buildchain-contract-drift-issue-mode: ${{ inputs.buildchain-contract-drift-issue-mode }}
      allow-repository: ${{ inputs.allow-repository }}
      channel: ${{ needs.resolve-promotion.outputs.publication-channel }}
      target-ref: ${{ needs.resolve-promotion.outputs.target-ref }}
      target-sha: ${{ inputs.target-sha }}
      package-manager: ${{ inputs.package-manager }}
      artifact-name: ${{ inputs.artifact-name }}
      release-candidate-workflow-file: ${{ inputs.release-candidate-workflow-file }}
      release-candidate-workflow-name: ${{ inputs.release-candidate-workflow-name }}
      release-candidate-wait-seconds: ${{ inputs.release-candidate-wait-seconds }}
      resume-candidate-repository: ${{ inputs.resume-candidate-repository }}
      resume-candidate-run-id: ${{ inputs.resume-candidate-run-id }}
      resume-expected-workflow-file: ${{ inputs.resume-expected-workflow-file }}
      resume-expected-workflow-name: ${{ inputs.resume-expected-workflow-name }}
      resume-expected-source-tree: ${{ inputs.resume-expected-source-tree }}
      resume-expected-candidate-root: ${{ inputs.resume-expected-candidate-root }}
      resume-expected-candidate-runtime-sha: ${{ inputs.resume-expected-candidate-runtime-sha }}
      resume-buildchain-runtime-sha: ${{ inputs.resume-buildchain-runtime-sha }}
      resume-transaction-id: ${{ inputs.resume-transaction-id }}
      release-candidate-family-evidence-required: ${{ inputs.release-candidate-family-evidence-required }}
      release-candidate-family-evidence-root: ${{ inputs.release-candidate-family-evidence-root }}
      release-candidate-family-initiative-id: ${{ inputs.release-candidate-family-initiative-id }}
      release-candidate-family-assignment-id: ${{ inputs.release-candidate-family-assignment-id }}
      artifact-patterns: ${{ inputs.artifact-patterns }}
      required-status-check: ${{ inputs.required-status-check }}
      branch-protection-bypass-apps: ${{ inputs.branch-protection-bypass-apps }}
      branch-protection-bypass-users: ${{ inputs.branch-protection-bypass-users }}
      branch-protection-bypass-teams: ${{ inputs.branch-protection-bypass-teams }}
      publish-target: ${{ inputs.publish-target }}
      github-release: ${{ inputs.github-release }}
      declarative-release-tail: ${{ inputs.declarative-release-tail }}
      github-release-title: ${{ inputs.github-release-title }}
      github-release-notes: ${{ inputs.github-release-notes }}
      github-release-payload-patterns: ${{ inputs.github-release-payload-patterns }}
      publication-commit-command: ${{ inputs.publication-commit-command }}
      publication-commit-evidence-path: ${{ inputs.publication-commit-evidence-path }}
      release-activation-command: ${{ inputs.release-activation-command }}
      release-activation-receipt-set-path: ${{ inputs.release-activation-receipt-set-path }}
      release-passport-evidence-command: ${{ inputs.release-passport-evidence-command }}
      release-passport-evidence-path: ${{ inputs.release-passport-evidence-path }}
      standalone-binary-distribution: ${{ inputs.standalone-binary-distribution }}
      runner-preset: ${{ inputs.runner-preset }}
      trusted-publishing: ${{ inputs.trusted-publishing }}
      publish-command: ${{ inputs.publish-command }}
      publish-rematerialize-on-resume: ${{ inputs.publish-rematerialize-on-resume }}
      publish-transaction-override: ${{ inputs.publish-transaction-override }}
      publish-required-artifacts-json: ${{ inputs.publish-required-artifacts-json }}
      publish-artifact-kind: ${{ inputs.publish-artifact-kind }}
      publish-mode: ${{ inputs.publish-mode }}
      publish-dist-tag: ${{ inputs.publish-dist-tag }}
      publish-package-set-order: ${{ inputs.publish-package-set-order }}
      publish-package-main: ${{ inputs.publish-package-main }}
      release-passport: ${{ inputs.release-passport }}
      release-propagation-config-path: ${{ inputs.release-propagation-config-path }}
      release-passport-output-dir: ${{ inputs.release-passport-output-dir }}
      release-passport-product-name: ${{ inputs.release-passport-product-name }}
      release-passport-impact-json: ${{ inputs.release-passport-impact-json }}
      release-passport-kfd-1-witness-jsons: ${{ inputs.release-passport-kfd-1-witness-jsons }}
      release-passport-kfd-2-claim-jsons: ${{ inputs.release-passport-kfd-2-claim-jsons }}
      release-passport-kfd-3-prebuild-witness-jsons: ${{ inputs.release-passport-kfd-3-prebuild-witness-jsons }}
      release-passport-kfd-3-artifact-witness-jsons: ${{ inputs.release-passport-kfd-3-artifact-witness-jsons }}
      release-passport-kfd-3-artifact-verify-command: ${{ inputs.release-passport-kfd-3-artifact-verify-command }}
      release-passport-kfd-adopter-manifest-json: ${{ inputs.release-passport-kfd-adopter-manifest-json }}
      release-passport-kfd-support-matrix-json: ${{ inputs.release-passport-kfd-support-matrix-json }}
      release-passport-kfd-product-gate-jsons: ${{ inputs.release-passport-kfd-product-gate-jsons }}
      release-passport-invariant-passport-jsons: ${{ inputs.release-passport-invariant-passport-jsons }}
      release-passport-invariant-passport-command: ${{ inputs.release-passport-invariant-passport-command }}
      release-passport-evidence-jsons: ${{ inputs.release-passport-evidence-jsons }}
      release-passport-attachment-command: ${{ inputs.release-passport-attachment-command }}
      release-passport-buildchain-self-kfd: ${{ inputs.release-passport-buildchain-self-kfd }}
      github-artifact-attestation-policy-json: ${{ inputs.github-artifact-attestation-policy-json }}
      github-artifact-attestation-environment: ${{ inputs.github-artifact-attestation-environment }}
      github-artifact-attestation-retention-days: ${{ inputs.github-artifact-attestation-retention-days }}
      release-passport-platform-manifest-paths: ${{ inputs.release-passport-platform-manifest-paths }}
      required-artifact-count: ${{ inputs.required-artifact-count }}
      publication-admission-json: ${{ inputs.publication-admission-json }}
      publication-runner-provenance-json: ${{ inputs.publication-runner-provenance-json }}
      publication-control-plane-audit-json: ${{ inputs.publication-control-plane-audit-json }}
      github-governance-receipt-json: ${{ inputs.github-governance-receipt-json }}
      publication-gate-aggregate-json: ${{ inputs.publication-gate-aggregate-json }}
      publication-gate-command: ${{ inputs.publication-gate-command }}
      publication-gate-controller-sha: ${{ inputs.publication-gate-controller-sha }}
      publication-expected-json: ${{ inputs.publication-expected-json }}
      publication-used-nonces-json: ${{ inputs.publication-used-nonces-json }}
      publication-consumer-qualification-command: ${{ inputs.publication-consumer-qualification-command }}
      publication-consumer-qualification-controller-sha: ${{ inputs.publication-consumer-qualification-controller-sha }}
      publication-consumer-predicate-id: ${{ inputs.publication-consumer-predicate-id }}
      publication-used-qualification-nonces-json: ${{ inputs.publication-used-qualification-nonces-json }}
      publication-auto-admission: ${{ inputs.publication-auto-admission }}
      publication-auto-no-gate: ${{ inputs.publication-auto-no-gate }}
      publication-publisher-workflow-path: ${{ inputs.publication-publisher-workflow-path }}
      publication-authority-workflow-path: .github/workflows/release-candidate-promote.yml
      publication-product: ${{ inputs.publication-product }}
      publication-target: ${{ inputs.publication-target }}
      publication-package-name: ${{ inputs.publication-package-name }}
      dry-run: ${{ inputs.dry-run }}
    secrets: inherit

  stable:
    name: Promote with stable workflow shell
    needs: resolve-promotion
    if: ${{ needs.resolve-promotion.outputs.channel == 'stable' }}
    uses: kungfu-systems/buildchain/.github/workflows/.release-candidate-promote.yml@98dc8e3d628b558bc8b35a91a821ad6c97334b77
    permissions:
      actions: write
      artifact-metadata: write
      attestations: write
      checks: write
      contents: write
      id-token: write
      issues: write
      pull-requests: write
    with:
      buildchain-repository: ${{ inputs.buildchain-repository }}
      buildchain-ref: ${{ needs.resolve-promotion.outputs.runtime-sha }}
      buildchain-expected-channel: ${{ needs.resolve-promotion.outputs.channel }}
      buildchain-expected-major: "4"
      promotion-router-ref: ${{ needs.resolve-promotion.outputs.router-ref }}
      promotion-router-sha: ${{ needs.resolve-promotion.outputs.router-sha }}
      promotion-shell-ref: ${{ needs.resolve-promotion.outputs.shell-call-ref }}
      promotion-shell-sha: ${{ needs.resolve-promotion.outputs.shell-sha }}
      promotion-runtime-ref: ${{ needs.resolve-promotion.outputs.runtime-ref }}
      promotion-runtime-sha: ${{ needs.resolve-promotion.outputs.runtime-sha }}
      promotion-contract-lock-path: ${{ needs.resolve-promotion.outputs.contract-lock-path }}
      promotion-contract-lock-digest: ${{ needs.resolve-promotion.outputs.contract-lock-digest }}
      promotion-publication-channel: ${{ needs.resolve-promotion.outputs.publication-channel }}
      promotion-target-ref: ${{ needs.resolve-promotion.outputs.target-ref }}
      promotion-override-used: ${{ needs.resolve-promotion.outputs.override-used == 'true' }}
      buildchain-contract-lock-path: ${{ needs.resolve-promotion.outputs.contract-lock-path }}
      buildchain-contract-compatibility-policy: ${{ inputs.buildchain-contract-compatibility-policy }}
      buildchain-contract-drift-issue-mode: ${{ inputs.buildchain-contract-drift-issue-mode }}
      allow-repository: ${{ inputs.allow-repository }}
      channel: ${{ needs.resolve-promotion.outputs.publication-channel }}
      target-ref: ${{ needs.resolve-promotion.outputs.target-ref }}
      target-sha: ${{ inputs.target-sha }}
      package-manager: ${{ inputs.package-manager }}
      artifact-name: ${{ inputs.artifact-name }}
      release-candidate-workflow-file: ${{ inputs.release-candidate-workflow-file }}
      release-candidate-workflow-name: ${{ inputs.release-candidate-workflow-name }}
      artifact-patterns: ${{ inputs.artifact-patterns }}
      required-status-check: ${{ inputs.required-status-check }}
      branch-protection-bypass-apps: ${{ inputs.branch-protection-bypass-apps }}
      branch-protection-bypass-users: ${{ inputs.branch-protection-bypass-users }}
      branch-protection-bypass-teams: ${{ inputs.branch-protection-bypass-teams }}
      publish-target: ${{ inputs.publish-target }}
      github-release: ${{ inputs.github-release }}
      declarative-release-tail: ${{ inputs.declarative-release-tail }}
      github-release-title: ${{ inputs.github-release-title }}
      github-release-notes: ${{ inputs.github-release-notes }}
      github-release-payload-patterns: ${{ inputs.github-release-payload-patterns }}
      publication-commit-command: ${{ inputs.publication-commit-command }}
      publication-commit-evidence-path: ${{ inputs.publication-commit-evidence-path }}
      standalone-binary-distribution: ${{ inputs.standalone-binary-distribution }}
      runner-preset: ${{ inputs.runner-preset }}
      trusted-publishing: ${{ inputs.trusted-publishing }}
      publish-command: ${{ inputs.publish-command }}
      publish-rematerialize-on-resume: ${{ inputs.publish-rematerialize-on-resume }}
      publish-transaction-override: ${{ inputs.publish-transaction-override }}
      publish-required-artifacts-json: ${{ inputs.publish-required-artifacts-json }}
      publish-artifact-kind: ${{ inputs.publish-artifact-kind }}
      publish-mode: ${{ inputs.publish-mode }}
      publish-dist-tag: ${{ inputs.publish-dist-tag }}
      publish-package-set-order: ${{ inputs.publish-package-set-order }}
      publish-package-main: ${{ inputs.publish-package-main }}
      release-passport: ${{ inputs.release-passport }}
      release-passport-output-dir: ${{ inputs.release-passport-output-dir }}
      release-passport-product-name: ${{ inputs.release-passport-product-name }}
      release-passport-impact-json: ${{ inputs.release-passport-impact-json }}
      release-passport-kfd-1-witness-jsons: ${{ inputs.release-passport-kfd-1-witness-jsons }}
      release-passport-kfd-2-claim-jsons: ${{ inputs.release-passport-kfd-2-claim-jsons }}
      release-passport-kfd-3-prebuild-witness-jsons: ${{ inputs.release-passport-kfd-3-prebuild-witness-jsons }}
      release-passport-kfd-3-artifact-witness-jsons: ${{ inputs.release-passport-kfd-3-artifact-witness-jsons }}
      release-passport-kfd-3-artifact-verify-command: ${{ inputs.release-passport-kfd-3-artifact-verify-command }}
      release-passport-invariant-passport-jsons: ${{ inputs.release-passport-invariant-passport-jsons }}
      release-passport-invariant-passport-command: ${{ inputs.release-passport-invariant-passport-command }}
      release-passport-buildchain-self-kfd: ${{ inputs.release-passport-buildchain-self-kfd }}
      release-passport-platform-manifest-paths: ${{ inputs.release-passport-platform-manifest-paths }}
      required-artifact-count: ${{ inputs.required-artifact-count }}
      publication-admission-json: ${{ inputs.publication-admission-json }}
      publication-runner-provenance-json: ${{ inputs.publication-runner-provenance-json }}
      publication-control-plane-audit-json: ${{ inputs.publication-control-plane-audit-json }}
      github-governance-receipt-json: ${{ inputs.github-governance-receipt-json }}
      publication-gate-aggregate-json: ${{ inputs.publication-gate-aggregate-json }}
      publication-expected-json: ${{ inputs.publication-expected-json }}
      publication-used-nonces-json: ${{ inputs.publication-used-nonces-json }}
      publication-consumer-qualification-command: ${{ inputs.publication-consumer-qualification-command }}
      publication-consumer-predicate-id: ${{ inputs.publication-consumer-predicate-id }}
      publication-used-qualification-nonces-json: ${{ inputs.publication-used-qualification-nonces-json }}
      publication-auto-admission: ${{ inputs.publication-auto-admission }}
      publication-auto-no-gate: ${{ inputs.publication-auto-no-gate }}
      publication-publisher-workflow-path: ${{ inputs.publication-publisher-workflow-path }}
      publication-product: ${{ inputs.publication-product }}
      publication-target: ${{ inputs.publication-target }}
      publication-package-name: ${{ inputs.publication-package-name }}
      dry-run: ${{ inputs.dry-run }}
    secrets: inherit
