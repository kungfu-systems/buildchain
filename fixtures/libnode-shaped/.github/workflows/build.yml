name: Libnode-shaped Buildchain Consumer

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  build:
    uses: kungfu-systems/buildchain/.github/workflows/build.yml@v4
    with:
      config-path: fixtures/libnode-shaped/buildchain.toml
